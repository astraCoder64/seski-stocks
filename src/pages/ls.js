import React, { Component } from 'react'
import SetForm from '../loginSignupForm/SetForm'

export default class Form extends Component {
    render() {
        return (
            <React.Fragment>
                <SetForm/>
            </React.Fragment>
        )
    }
}
